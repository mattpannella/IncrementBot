services:
  inc-bot:
    image: inc-bot-image
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Inc-Bot
    volumes:
      - type: bind
        source: ${STATE_DIR}/state.json
        target: /App/state.json